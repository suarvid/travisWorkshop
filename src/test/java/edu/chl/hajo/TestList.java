package edu.chl.hajo;

import static java.lang.System.out;
import static org.junit.Assert.*;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

/*
    
     This is a test class (for some other class, in this case the List class)

 */
public class TestList {
/*
    @Test
    public void testAdd() {
        List l = new List();
        l.add(1);
        assertTrue(l.getLength() == 1);
    }

    @Test
    public void testRemove() {
        List l = new List();
        l.add(2);
        l.add(3);
        l.add(4);
        int i = l.remove();
        assertEquals(4, i);
        assertTrue(l.getLength() == 2);
    }


    @Test
    public void testGet(){
        List list = new List();
        list.add(0);
        list.add(1);
        list.add(2);
        list.add(3);
        list.add(4);
        int value = list.get(2);
        assertEquals(2, value);
    }

    @Test(expected = IllegalArgumentException.class)
    public void testGetBadIndex() {
        List list = new List();
        list.get(-2);
    }


    @Test
    public void testCopy() {
        List listOne = new List();
        listOne.add(1);
        listOne.add(2);
        listOne.add(3);

        List listTwo = listOne.copy();
        assertEquals(listOne.getLength(),listTwo.getLength());
        assertEquals(listOne.get(0),listTwo.get(2));
        assertEquals(listOne.get(1),listTwo.get(1));
        assertEquals(listOne.get(2),listTwo.get(0));

    }

    @BeforeClass
    public static void beforeClass() {
        System.out.println("Before class");
    }

    @AfterClass
    public static void afterClass() {
        System.out.println("After class");
    }

    @Before
    public void before() {
        System.out.println("Before Test");
    }

    @After
    public void after() {
        System.out.println("After Test");
    }
*/
}
